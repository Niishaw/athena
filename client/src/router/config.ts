import { iConfig } from "../types/router";

const config:iConfig = {
    layouts: {
        home: {
            heading: "Athena Credit Cards",
            appBarColor: "primary"
        }
    }
};

export default config;